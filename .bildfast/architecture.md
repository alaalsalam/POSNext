# Architecture

- Frappe app path: `/home/frappe/frappe-bench/apps/pos_next`.
- Primary installed site: `pos.yemenfrappe.com`.
- Main route: `/desk`.
- Platform project record: `LP-00022` with `source_type = Frappe App` and `provision_status = Ready`.

Use Frappe framework APIs and existing DocTypes/controllers. Preserve local branches and uncommitted work.
